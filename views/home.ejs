<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CHENab Valley</title>
  <link rel="icon" type="image/png" sizes="32x32" href="https://img.icons8.com/?size=100&id=iy3LRN9x9HEg&format=png&color=000000" />
  <style>
    /* Basic Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Navbar Styling */
.navbar {
  background: linear-gradient(45deg, #4b0082, #8a2be2);
  padding: 1rem;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
  position: sticky;
  top: 0;
  z-index: 1000;
}

.navbar-menu {
  display: flex;
  justify-content: center;
  gap: 2rem;
  list-style: none;
}

.navbar-menu li a {
  color: #fff;
  text-decoration: none;
  font-weight: bold;
  font-size: 1.2rem;
  padding: 0.5rem 1.5rem;
  border: 2px solid transparent;
  border-radius: 10px;
  transition: all 0.3s ease-in-out;
}

.navbar-menu li a:hover {
  background: #fff;
  color: #4b0082;
  border: 2px solid #4b0082;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
}

/* Body Styling */
body {
  font-family: 'Arial', sans-serif;
  background: linear-gradient(to bottom, #f8f9fa, #e0e0e0);
  color: #333;
  padding: 1rem;
  text-align: center;
  animation: fadeIn 2s ease-in-out;
}

h1 {
  font-size: 2.5rem;
  color: #4b0082;
  margin-bottom: 1.5rem;
  text-transform: uppercase;
  text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
  animation: slideIn 1s ease-in-out;
}

/* Container Styling */
.container {
  width: 90%;
  margin: 2rem auto;
  background: rgba(255, 255, 255, 0.9);
  padding: 1.5rem;
  border-radius: 15px;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  transition: transform 0.3s ease-in-out;
}

.container:hover {
  transform: scale(1.02);
}

/* Scrolling Wrapper */
.scrolling-wrapper {
  display: flex;
  overflow-x: auto;
  gap: 1rem;
  padding: 1rem;
  scroll-snap-type: x mandatory;
  scroll-behavior: smooth;
  animation: fadeInUp 1s ease;
}

.card {
  min-width: 220px;
  height: 150px;
  background: linear-gradient(45deg, #8a2be2, #4b0082);
  color: white;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 12px;
  scroll-snap-align: start;
  font-size: 1.2rem;
  font-weight: bold;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  cursor: pointer;
}

/* Horizontal Container */
.horizontal-container {
  display: flex;
  overflow-x: auto;
  background: rgba(255, 255, 255, 0.9);
  gap: 1rem;
  padding: 1rem;
  border-radius: 15px;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  animation: fadeInUp 1.2s ease;
}

.horizontal-container div {
  min-width: 150px;
  padding: 1rem;
  background: linear-gradient(45deg, #8a2be2, #4b0082);
  color: white;
  text-align: center;
  border-radius: 12px;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.horizontal-container div:hover {
  transform: translateY(-10px);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
}

/* Grid Container */
.grid-container {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem;
  padding: 1rem;
  border-radius: 15px;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  max-width: 900px;
  margin: 2rem auto;
  animation: fadeInUp 1.5s ease;
}

.grid-container div {
  background: linear-gradient(45deg, #4b0082, #8a2be2);
  color: white;
  padding: 1rem;
  text-align: center;
  border-radius: 12px;
  font-size: 1rem;
  font-weight: bold;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.grid-container div:hover {
  transform: scale(1.05);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
  cursor: pointer;
}

/* Footer */
footer {
  margin-top: 2rem;
  font-size: 0.9rem;
  color: #555;
  padding-top: 1rem;
  border-top: 1px solid #ccc;
  animation: fadeInUp 2s ease;
}

/* Keyframes */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideIn {
  from {
    transform: translateX(-100%);
  }
  to {
    transform: translateX(0);
  }
}

/* Responsive Styles */
@media (max-width: 768px) {
  .navbar-menu {
    flex-direction: column;
    gap: 1rem;
  }

  .grid-container {
    grid-template-columns: 1fr;
  }
}

  

  </style>
</head>
<body>
  <nav class="navbar">
    <ul class="navbar-menu" >
      <li><a href="/">Chenab Valley</a></li>
      <li><a href="#about">About</a></li>
<li></li>
<li></li>
   
     <li><a href="/login">Employee Login</a></li>
    </ul>
  </nav>
  <h1>Welcome to CHENab Valley</h1>
  <div class="container" >
    <marquee behavior="" direction="">
        <div class="scrolling-wrapper"id="top1">
      
        </div>
    </marquee>
  </div>
  <h1>Latest Happenings in Chenab Valley</h1>
  <div style="display: flex;">
    <div class="grid-container" id="top2">
        
      </div>
    <div id="sidediv">qisjqjsj</div>
  </div>
  <div class="horizontal-container">
    <div>Event 1</div>
    <div>Event 2</div>
    <div>Event 3</div>
    <div>Event 4</div>
  </div>
  <marquee>WELCOME TO CHENab VALLEY. WEBSITE IS READY TO WORK</marquee>
  <p>This is a sample page with a navigation bar.</p>

  <about id="about">About with teamto create the latest update of the area ...........</about>
  <footer>Terms and Conditions</footer>


  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <!-- //axios -->

  <script src="/browser.js"></script>
</body>
</html>
