<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      /* Reset some default styling */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      /* Body styling */
      body {
        font-family: Arial, sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-color: #f7f9fc;
      }

      /* Center container styling */
      .container {
        max-width: 400px;
        width: 100%;
        padding: 20px;
        background-color: #ffffff;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
      }

      /* Title styling */
      h1 {
        color: #333;
        font-size: 24px;
        text-align: center;
        margin-bottom: 20px;
      }

      /* Form styling */
      form {
        display: flex;
        flex-direction: column;
      }

      label {
        font-size: 14px;
        margin-top: 10px;
        color: #555;
      }

      input[type="file"],
      input[type="text"] {
        padding: 8px;
        font-size: 14px;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin-top: 5px;
      }

      button {
        padding: 10px;
        font-size: 16px;
        background-color: #4caf50;
        color: white;
        border: none;
        border-radius: 4px;
        margin-top: 20px;
        cursor: pointer;
      }

      button:hover {
        background-color: #45a049;
      }

      /* Top1 section styling */
      top1 h1 {
        text-align: center;
        font-size: 20px;
        color: #4caf50;
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Backend Work</h1>
      <top1>
        <h1>Top1 Data</h1>
        <form method="post" action="/createtop1text" enctype="multipart/form-data">
            <label for="img">Image</label>
            <input id="img" name="image" type="file" placeholder="top1 data" required /><br />
            
            <label for="head">Head Title</label>
            <input id="head" name="head" type="text" maxlength="15" placeholder="head text" required />
            
            <label for="textbody">Text Body</label>
            <input id="textbody" name="textbody" type="text" maxlength="55" placeholder="text" required />
            
            <button type="submit">Submit</button>
          </form>
          
      </top1>
      <h1>TOP2 DATA ADDED</h1>
      <top2>
        <form method="post" action="/createtop2text" enctype="multipart/form-data">
            <label for="img">Image</label>
            <input id="img" name="image" type="file" placeholder="top1 data" required /><br />
            
            <label for="head">Head Title</label>
            <input id="head" name="head" type="text" maxlength="15" placeholder="head text" required />
            
            <label for="textbody">Text Body</label>
            <input id="textbody" name="textbody" type="text" maxlength="55" placeholder="text" required />
            
            <button type="submit">Submit</button>
          </form>
        
      </top2>
    </div>
  </body>
</html>

